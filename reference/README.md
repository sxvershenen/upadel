# Reference snapshot

`prototype-baseline` is an untouched copy of the supplied React/Vite prototype, excluding dependencies and reproducible build output. It exists only for migration comparison and recovery.

Never edit this directory. Implement production changes under `apps/*` and compare layout, responsive behavior and animations against the baseline.
